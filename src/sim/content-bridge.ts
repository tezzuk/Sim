// Re-exports of content tables for sim modules (keeps import paths tidy and
// avoids deep relative paths from src/sim/systems/*).
export { BUILDINGS, STARTING_BUILDINGS } from '../content/buildings';
export { JOBS } from '../content/jobs';
export { TRAITS } from '../content/traits';
